# Core dependencies
yt-dlp>=2026.07.04
# STT: faster-whisper (CTranslate2, no torch) replaces openai-whisper
faster-whisper>=1.1.0
# OCR: RapidOCR (ONNX Runtime, ~15 MB) replaces easyocr (~94 MB + torch)
rapidocr-onnxruntime>=1.4.0
Pillow>=10.0.0
moviepy>=1.0.3
instaloader>=4.10
requests>=2.31.0
pandas>=2.0.0
openpyxl>=3.1.0

# PornHub / impersonation support (required for sites that block bots)
curl_cffi>=0.15.0

# Gemini script generator
google-genai>=2.0.0
# Service-account auth for Gemini (Case Commentary / Script Studio tabs).
# REQUIRED — without these the AI tabs throw "google-auth library required".
google-auth>=2.23.0
google-auth-httplib2>=0.1.1

# PyTorch REMOVED — the app is now torch-free. Heavy ML (torch) is not used
# by this tool's OCR/STT pipeline (RapidOCR + faster-whisper).
